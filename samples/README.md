# Sample using Postgres database


## Run

First export the following environment variables:

```
$ export DB_USER=
$ export DB_PASSWORD=
$ export DB_NAME=
```

Then:

```
$ npm start <pg|mysql|...>
```
